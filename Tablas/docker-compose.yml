version: "3.9"
services:
analytics:
build:
context: .
dockerfile: docker/Dockerfile
image: incidents-analytics:latest
container_name: incidents-analytics
volumes:
- ./data:/app/data:ro
- ./outputs:/app/outputs
deploy:
resources:
limits:
cpus: '2.0'